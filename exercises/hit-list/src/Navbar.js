import React from "react";

function Navbar() {
  const styles = {
    backgroundColor: "coral",
    height: "100px",
    width: "100%"
  }
  return (
    <div style={styles}>
      <h2>Hit List</h2>
    </div>
  )
}

export default Navbar;
